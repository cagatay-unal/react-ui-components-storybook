import { Canvas, Meta, Story, ArgsTable} from '@storybook/addon-docs';
import { Input } from './Input';

<Meta title="Components/Form Elements/Input" component={Input} />

export const Template = (args) => <Input {...args} />;

<div class="section-title">
    <h4>input element</h4>
    <p class="description">The development process of this <span className="badge badge-success">Form Element / Input</span> component is ongoing.</p>
</div>


<Canvas>
    <Story name="Default Input" args={{ type: 'text', status: 'default', placeholderText: 'placeholder text'}}>
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <ArgsTable story="Default Input" />
</Canvas>